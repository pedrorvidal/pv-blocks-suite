// Fully dynamic block: render.php builds all markup from attributes, so
// nothing needs to be stored as post content (see cta/save.tsx for the
// same pattern and full explanation).
export default function save() {
    return null;
}
